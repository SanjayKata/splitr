# Normalize line endings: store everything as LF in the repo.
# Prevents CRLF/LF churn between Windows dev and Linux CI.
* text=auto eol=lf

# Binary files — never modify.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.webp binary
*.woff binary
*.woff2 binary
